<UserControl x:Class="GoldSignalAnalyzer.Desktop.Views.RiskPlanView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <StackPanel VerticalAlignment="Top">
            <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" TextWrapping="Wrap" />
            <Border Margin="0,16,0,0" Padding="12,8" CornerRadius="4"
                    Background="#FFF4E5" BorderBrush="#E0A030" BorderThickness="1">
                <TextBlock Text="{Binding RoadmapBanner}" Foreground="#8A5A00" TextWrapping="Wrap" />
            </Border>
            <TextBlock Margin="0,16,0,0" Foreground="#888888" TextWrapping="Wrap"
                       Text="{Binding Traceability, StringFormat='Traces to: {0}'}" />
        </StackPanel>
    </Grid>
</UserControl>
