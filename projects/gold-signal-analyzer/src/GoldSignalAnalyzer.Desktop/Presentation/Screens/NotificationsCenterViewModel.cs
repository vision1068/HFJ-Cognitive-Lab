namespace GoldSignalAnalyzer.Desktop.Presentation.Screens;

/// <summary>§30 screen "Notifications Center" (traces to FR-30, §31). Cycle-1 navigable placeholder (arch §7).</summary>
public sealed class NotificationsCenterViewModel : PlaceholderScreenViewModel
{
    public NotificationsCenterViewModel()
        : base("Notifications Center", "FR-30, §31")
    {
    }
}
