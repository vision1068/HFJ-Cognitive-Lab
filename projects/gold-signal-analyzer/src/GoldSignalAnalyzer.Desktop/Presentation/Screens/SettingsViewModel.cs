namespace GoldSignalAnalyzer.Desktop.Presentation.Screens;

/// <summary>§30 screen "Settings" (traces to FR-6, §37). Cycle-1 navigable placeholder (arch §7).</summary>
public sealed class SettingsViewModel : PlaceholderScreenViewModel
{
    public SettingsViewModel()
        : base("Settings", "FR-6, §37")
    {
    }
}
