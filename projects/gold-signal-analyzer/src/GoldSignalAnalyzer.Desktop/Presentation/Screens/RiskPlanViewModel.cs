namespace GoldSignalAnalyzer.Desktop.Presentation.Screens;

/// <summary>§30 screen "Risk Plan" (traces to FR-24, §25). Cycle-1 navigable placeholder (arch §7).</summary>
public sealed class RiskPlanViewModel : PlaceholderScreenViewModel
{
    public RiskPlanViewModel()
        : base("Risk Plan", "FR-24, §25")
    {
    }
}
