namespace GoldSignalAnalyzer.Desktop.Presentation.Screens;

/// <summary>§30 screen "Backtest Results / Report" (traces to FR-31, §32). Cycle-1 navigable placeholder (arch §7).</summary>
public sealed class BacktestResultsViewModel : PlaceholderScreenViewModel
{
    public BacktestResultsViewModel()
        : base("Backtest Results / Report", "FR-31, §32")
    {
    }
}
