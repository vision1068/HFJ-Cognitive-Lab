namespace GoldSignalAnalyzer.Desktop.Presentation.Screens;

/// <summary>§30 screen "Market Regime" (traces to FR-16, §16). Cycle-1 navigable placeholder (arch §7).</summary>
public sealed class MarketRegimeViewModel : PlaceholderScreenViewModel
{
    public MarketRegimeViewModel()
        : base("Market Regime", "FR-16, §16")
    {
    }
}
