namespace GoldSignalAnalyzer.Desktop.Presentation.Screens;

/// <summary>§30 screen "Signal Detail / Explanation" (traces to FR-23, §24). Cycle-1 navigable placeholder (arch §7).</summary>
public sealed class SignalDetailViewModel : PlaceholderScreenViewModel
{
    public SignalDetailViewModel()
        : base("Signal Detail / Explanation", "FR-23, §24")
    {
    }
}
